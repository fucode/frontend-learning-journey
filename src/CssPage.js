import React from 'react';

function CssPage() {
  return (
    <section className="section">
      <h2>CSS</h2>
      <p>
        CSS (Cascading Style Sheets) is all about styling. I’m learning about selectors, the box model,
        Flexbox, Grid, media queries, and animations.
      </p>
      <footer className="footer">CSS Learning Section</footer>
    </section>
  );
}

export default CssPage;
